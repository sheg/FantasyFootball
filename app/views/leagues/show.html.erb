<h1> <%= @league.name.titleize %> </h1>

<table class="table table-responsive table-condensed">
  <th>Team Name</th>
  <tbody>
  <% @teams.each do |team| %>
      <tr>
        <td><%= link_to team.name, '#' %></td>
      </tr>
  <% end %>
  </tbody>
</table>